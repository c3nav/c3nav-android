language: android
sudo: false
android:
    components:
        - tools
        - platform-tools
        - build-tools-25.0.0
        - android-25
        - extra-android-m2repository
env:
    - TERM=dumb
script:
    - cd CongressRoutePlanner
    - ./gradlew check lint
    - cat app/build/outputs/lint-results.xml
cache:
    directories:
        - $HOME/.gradle
